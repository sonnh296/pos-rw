global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  # Spring Boot application metrics (JVM threads, memory, GC, HTTP latency)
  - job_name: 'pos-boost'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['boost:8080']
    scrape_interval: 2s
